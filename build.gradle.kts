plugins {
    // IMPORTANT: Replace "8.2.0" and "1.9.20" with the actual versions used in your project
    // if they are different. These are common default versions.
    id("com.android.application") version "8.11.1" apply false
    id("org.jetbrains.kotlin.android") version "2.2.0" apply false
    id("org.jetbrains.kotlin.plugin.compose") version "2.2.0" apply false // Ensure this version matches kotlin.android
}
